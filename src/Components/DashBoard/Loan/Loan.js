import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class Loan extends Component {
  render() {
    return (
      <View style={{ flex: 1, backgroundColor: '#FF967C' }}>
      <Text> Loan Component</Text>
      </View>
    );
  }
}
